{
  "name": "call-once-after-tick",
  "version": "1.0.2",
  "description": "Enables multiple call-sites to enqueue a function call that will occur both asynchronously and only once per tick",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha --require source-map-support/register --reporter spec 'lib/**/tests/*.js'",
    "_test": "mocha --reporter spec 'lib/**/tests/*.js'",
    "lint": "eslint runtime/*.js src/**",
    "build": "./scripts/build.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/markfinger/call-once-after-tick.git"
  },
  "bugs": {
    "url": "https://github.com/markfinger/call-once-after-tick/issues"
  },
  "homepage": "https://github.com/markfinger/call-once-after-tick#readme",
  "author": "Mark Finger",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-eslint": "^6.0.0",
    "babel-preset-es2015": "^6.5.0",
    "chai": "^3.5.0",
    "eslint": "^3.0.0",
    "mocha": "^2.4.5",
    "source-map-support": "^0.4.0",
    "yargs": "^4.1.0"
  }
}
